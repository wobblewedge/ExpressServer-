//needs a variable that points to the url of the desired article. make html that has a button and shit.

fetch(url)
.then(function(response) {
  // get body of article and assign it to a global? variable
})
.catch(function(error) {
  console.log('Looks like there was a problem: \n', error);
});